import React from 'react'
import { millik, mukta } from '@/app/font'

function Section() {
  return (
    <div className={`max-w-[1152px] m-auto `}>
        <div className={`bg-[#0A2656] font-[500]  max-w-[1104px] text-[24px] p-[24px] rounded-[20px]  text-[#FFFFFF] ${mukta.className} text-center `}>
        We do this because we want to help business scale fast, develop novelty solutions and solve unique business problem. If you can dream it, we can build it.
        </div>
    </div>
  )
}

export default Section